import React from 'react';
// import UserLists from './UsersList'

const Main = (props) => {
    const { data } = props;
    return (
        <div className="container">
            {/* <UsersList posts={users} /> */}
        </div>
    )
}

export default Main;
